<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    
    <meta charset="utf-8">
 
    <title>index</title>
  </head>
  <body>
    <h1>Driver index page</h1>
    <nav>
        <a href="/drivers/new">Create a New Driver </a>
    </nav>
    <ul>
      <% for(let i = 0; i < drivers.length; i++) { %>
        <li>
          <a href="/drivers/<%= drivers[i].id %>"><%=drivers[i].name %> </a>
              <form action="/drivers/<%= drivers[i].id %>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE"/>
            </form>
            <a href="/drivers/<%= drivers[i]._id %>/edit"> Edit</a>
        </li>
      <% } %>
    </ul>
   
  </body>
</html>