<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    
    <meta charset="utf-8">
 
    <title>index</title>
  </head>
  <body>
    <h1>Company index page</h1>
    <nav>
        <a href="/companies/new">Create a New Company </a>
    </nav>
    <ul>
      <% for(let i = 0; i < companies.length; i++) { %>
        <li>
          <a href="/companies/<%= companies[i].id %>"><%=companies[i].name %> </a>
              <form action="/companies/<%= companies[i].id %>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE"/>
            </form>
            <a href="/companies/<%= companies[i]._id %>/edit"> Edit</a>
        </li>
      <% } %>
    </ul>
   
  </body>
</html>

